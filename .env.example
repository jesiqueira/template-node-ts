# .env.example (para seu template)
# =======================================================
# Variáveis do Projeto (Padrão)
# =======================================================
PORT=3000
NODE_ENV=development

# =======================================================
# CONFIGURAÇÃO DO BANCO DE DADOS (POSTGRESQL)
# =======================================================
DB_HOST=database
DB_USER=dev
DB_PASSWORD=dev123
DB_NAME=myapp
DB_PORT=5432

# =======================================================
# CONFIGURAÇÃO DO JWT (JSON WEB TOKEN)
# =======================================================
JWT_SECRET=change_this_to_a_secure_secret_key
JWT_EXPIRES_IN=7d
