# .env (arquivo REAL que você cria a partir do .env.example)
# =======================================================
# Variáveis do Projeto (Padrão)
# =======================================================
PORT=3000
NODE_ENV=development

# =======================================================
# CONFIGURAÇÃO DO BANCO DE DADOS (POSTGRESQL)
# =======================================================
DB_HOST=database  # ← NOME DO SERVICE NO docker-compose!
DB_USER=dev
DB_PASSWORD=dev123
DB_NAME=myapp
DB_PORT=5432

# =======================================================
# CONFIGURAÇÃO DO JWT (JSON WEB TOKEN)
# =======================================================
JWT_SECRET=change_this_to_a_secure_secret_key_in_production  # ⚠️ MUDAR!
JWT_EXPIRES_IN=7d

# Gere uma secret única
# openssl rand -base64 64
# Cole no .env do NOVO projeto